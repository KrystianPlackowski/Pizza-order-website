{% extends "orders/base.html" %}

{% block title %}
    Menu 
{% endblock %}

{% block body %}
    <h1 style="padding-left:15%">Menu</h1>

    {% for foodkind in menu %}
        <h2>{{ foodkind.name }}</h2>
        <table class="mytable">
            <tr>
                <th></th>
                {% for xlabel in foodkind.xlabels %}
                    <th>{{ xlabel }}</th>
                {% endfor %}
            </tr>
            {% for row in foodkind.body %}
                <tr>
                {% for item in row|slice:":1" %}
                    <td>{{ item }}</td>
                {% endfor %}
                {% for item in row|slice:"1:" %}
                    {% if item.price > 0 %}
                        <td>
                            <a href="{% url 'item_info' item.id %}">
                                {{ item.price }}
                            </a>
                        </td>
                    {% else %}
                        <td></td>
                    {% endif %}
                {% endfor %}
                </tr>
            {% endfor %}
        </table>
    {% endfor %}

    <h2>Toppings</h2>
    <ul>
    {% for topping in toppings %}
        <li>{{ topping }}</li>
    {% endfor %}
    </ul>

{% endblock %}
